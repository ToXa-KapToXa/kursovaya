spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/Kursovaya
    username: postgres
    password: 2504
  flyway:
    baseline-on-migrate: true